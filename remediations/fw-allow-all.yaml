apiVersion: compute.gcp.upbound.io/v1beta1
kind: Firewall
metadata:
  name: fw-allow-all
spec:
  providerConfigRef:
    name: gcp-provider
  forProvider:
    project: sre-agent-hack
    name: fw-allow-all
    sourceRanges:
    - 10.0.0.0/8